<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- 字符编码过滤器 -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>com.boda.filter.EncodingFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 登录检查过滤器 -->
    <filter>
        <filter-name>LoginFilter</filter-name>
        <filter-class>com.boda.filter.LoginFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>LoginFilter</filter-name>
        <url-pattern>/chat.jsp</url-pattern>
        <url-pattern>/sendMessage</url-pattern>
        <url-pattern>/logout</url-pattern>
    </filter-mapping>

    <!-- 上下文监听器 -->
    <listener>
        <listener-class>com.boda.listener.ChatContextListener</listener-class>
    </listener>

    <!-- 会话监听器 -->
    <listener>
        <listener-class>com.boda.listener.ChatSessionListener</listener-class>
    </listener>

    <!-- 欢迎页面 -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

</web-app>